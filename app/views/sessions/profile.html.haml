%h1 Welcome #{current_user.to_s}, to your profile page!
%p Here, you'll be allowed to view, edit, and destroy your posts
%h2 Some stats for you
%p
  Total number of your posts : #{current_user.posts.count}
%p
  Democracy level (votes given) : #{current_user.votes.size}
%p
  Trust score : #{current_user.trustscore}

%table
  %thead
    %tr
      %th Score
      %th Title
      %th Contents
      %th Author
      %th{:colspan => "3"}
  %tbody
    - current_user.posts.each do |post|
      %tr
        %td= post.score
        %td= post.title
        %td= post.contents
        %td= post.user.to_s
        %td= link_to 'Open', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
