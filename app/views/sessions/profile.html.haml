%h1 
  #{@user.to_s}'s profile page
%p
  - if current_user == @user 
    Here, you'll be allowed to view, edit, and destroy your posts
  - else
    Here you can look at #{@user.to_s}'s posts
%h2 
  Some stats for you
%p
  Total number of posts : #{@user.posts.count}
%p
  Democracy level (votes given) : #{@user.votes.size}
%p
  Trust score : #{@user.trustscore}

%table
  %thead
    %tr
      %th Score
      %th Title
      %th Contents
      %th Author
      %th{:colspan => "3"}
  %tbody
    - @user.posts.each do |post|
      %tr
        %td= post.score
        %td= post.title
        %td= post.contents
        %td= post.user.to_s
        %td= link_to 'Open', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
