.commentish.comment{id: comment.html_id}
  .head
    = link_to comment.user, comment.user, class: 'user'
    = link_to "#{time_ago_in_words comment.created_at} ago", comment, class: 'date'
  .content= comment.contents
