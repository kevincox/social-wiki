%p.commentish.comment{id: comment.html_id}
  %span.content= comment.contents
  != '&nbsp;—&nbsp;'
  %span.user= link_to comment.user, comment.user
  != '&nbsp;'
  %span.date= link_to "#{time_ago_in_words comment.created_at} ago", comment
